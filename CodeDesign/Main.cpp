// The HelloWorld test program

#include <iostream>

int main() {
	//Create strings for inputs from the user
	//Create an array for commands that are acceptable (get, go, use, etc.)

	//initialize rooms
	//initialize items

	// Display game rules

	//Request for command

	/*
	this should check the players input and confirm if its valid.
	While its valid it will decide what should happen next.
	while(checkCommand(inputString)){
		if(inputString == quit){
		exit game
		}
		elseif(input contains go){
		
		}
		.
		.
		.
	}
	*/
}
//Anthony Gonzalez

/*
boolean checkCommand(string playerCommand){
	for(acceptable commands){
		if(playerCommand = accetableCommand){
		return true;
		}
	}
	return false;
}*/
